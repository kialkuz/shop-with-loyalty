WithDraw:
  type: object
  required:
    - order
    - sum
  properties:
    order:
      type: string
      description: Order number associated with the withdrawal.
      example: "12345678903"

    sum:
      type: number
      format: double
      description: Amount of loyalty points to withdraw.
      example: 100.50
